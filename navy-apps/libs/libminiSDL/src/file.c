#include <sdl-file.h>

SDL_RWops* SDL_RWFromFile(const char *filename, const char *mode) {
  printf("SDL_RWFromFile have no implement!\n");
  assert(0);
  return NULL;
}

SDL_RWops* SDL_RWFromMem(void *mem, int size) {
  printf("SDL_RWFromMem have no implement!\n");
  assert(0);
  return NULL;
}
